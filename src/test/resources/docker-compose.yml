version: '2'

services:
  ovsdb:
    image: simplyti/ovsdb:2.8.1-onbuild
    ports:
    - 6641:6641
    command:
    - --remote=ptcp:6641:0.0.0.0
    - --remote=punix:/usr/local/var/run/openvswitch/ovnnb_db.sock
    - /usr/local/etc/openvswitch/ovnnb_db.db 